{% extends "templates/pageModel.njk" %}

{% block content %}
<div class="container-fluid mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-header {% if disabled %}bg-info{% else %}bg-warning{% endif %} text-white">
                    <h4 class="mb-0">
                        {% if disabled %}
                        <i class="bi bi-eye"></i> {{ title }}
                        {% else %}
                        <i class="bi bi-pencil"></i> {{ title }}
                        {% endif %}
                    </h4>
                </div>
                <div class="card-body">
                    <form id="formAutor">
                        <input type="hidden" id="autorid" name="autorid" value="{{ data.autorid }}">
                        
                        <div class="mb-3">
                            <label for="nome" class="form-label">Nome *</label>
                            <input type="text" class="form-control" id="nome" name="nome" 
                                   value="{{ data.nome }}" {% if disabled %}disabled{% endif %} required>
                        </div>

                        <div class="mb-3">
                            <label for="nacionalidade" class="form-label">Nacionalidade *</label>
                            <input type="text" class="form-control" id="nacionalidade" name="nacionalidade" 
                                   value="{{ data.nacionalidade }}" {% if disabled %}disabled{% endif %} required>
                        </div>

                        <div id="alertMsg" class="alert d-none" role="alert"></div>

                        <div class="d-flex justify-content-between">
                            <a href="/autores" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Voltar
                            </a>
                            {% if not disabled %}
                            <button type="submit" class="btn btn-warning">
                                <i class="bi bi-save"></i> Atualizar
                            </button>
                            {% endif %}
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

{% if not disabled %}
<script>
document.getElementById('formAutor').addEventListener('submit', async (e) => {
    e.preventDefault();
    
    const formData = {
        autorid: parseInt(document.getElementById('autorid').value),
        nome: document.getElementById('nome').value,
        nacionalidade: document.getElementById('nacionalidade').value,
        removido: false
    };
    
    const alertMsg = document.getElementById('alertMsg');
    
    try {
        const response = await fetch('/autores/update', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify(formData)
        });
        
        const data = await response.json();
        
        if (data.status === 'ok') {
            alertMsg.className = 'alert alert-success';
            alertMsg.textContent = 'Autor atualizado com sucesso!';
            alertMsg.classList.remove('d-none');
            
            setTimeout(() => {
                window.location.href = '/autores';
            }, 1500);
        } else {
            alertMsg.className = 'alert alert-danger';
            alertMsg.textContent = 'Erro: ' + data.msg;
            alertMsg.classList.remove('d-none');
        }
    } catch (error) {
        alertMsg.className = 'alert alert-danger';
        alertMsg.textContent = 'Erro ao atualizar autor';
        alertMsg.classList.remove('d-none');
    }
});
</script>
{% endif %}
{% endblock %}
